# OpenAI Configuration (REQUIRED for RAG service)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4.1-mini

# STT Configuration
# Options: "cuda" (for GPU) or "cpu" (for CPU-only)
WHISPER_DEVICE=cuda

# RAG Configuration
# Base URL for scraping campaigns (optional, has default)
CEPTETEB_URL=https://www.cepteteb.com.tr

# Service URLs (for local development)
# These are only needed if running services separately
# For Docker, these are set automatically in docker-compose.yml
STT_SERVICE_URL=http://localhost:8001
RAG_SERVICE_URL=http://localhost:8002

